using System;

namespace lp2_rec_ghosts.Model
{
    /// <summary>
    /// The colors that characterize every element on the board. 
    /// </summary>
    public enum Colors
    {
        RED,
        YELLOW,
        BLUE,
        MIRROR,
        BLOCK
    }
}